using employe.appmodel.models;

namespace employe.appmodel
{
    internal class Program
    {
        static void Main(string[] args)
        {
//            Create an abstract class Employee with:
//o Private properties: EmployeeId, Name, BasicSalary
//o Constructor to initialize them
//o Public methods:
// GetEmployeeId(), GetName(), GetBasicSalary()
// SetBasicSalary(double amount) to update salary with validations
// An abstract method CalculateSalary() to be implemented by child
//classes

            var emp =new FullTimeEmploye(...);

            Console.WriteLine( emp.calculatesalary() );
        }
    }
}
